package code.studyhalls.st04_02_2022;

public class ThreadSafety {

    /*
    When only one thread is allowed to access an object or a piece of code
    at a time, that object or that piece of code is called thread-safe
    or synchronized; otherwise it's called non-synchronized.

    ArrayList is non-synchronized. It means that an object of ArrayList is not
    thread safe. Multiple threads can access it at the same time(concurrently).
     */
}
